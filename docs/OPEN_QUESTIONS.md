# Open questions

Track questions and their decisions here, retaining resolved rows for reference. Proposals in the
[concept discussion](brainstorming/context-switching-app-concept.md) are inputs, not defaults.
When a decision is made, record it and its rationale in the relevant phase document, update
its status and outcome here, and update affected plans. Summarize the outcome briefly and link
to the decision details. A proposed option is not an accepted decision.

Question status is one of `Open`, `Investigating`, `Resolved`, or `Deferred`. For deferred
questions, record why and when to revisit them in the outcome. These describe questions;
phase status remains exclusively in the roadmap. Related phases are optional: use `—` when
none has been assigned, and link to existing phase documents when applicable.

| ID | Decision needed | When needed | Current input | Status | Outcome | Related phases |
| --- | --- | --- | --- | --- | --- | --- |
| Q01 | Is Konzendi the public product name? | Before public branding or package identifiers | Workspace name only; concept leaves naming open. | Resolved | Yes. Konzendi is the public product name and is used for package and bundle identifiers; the bundle identifier `com.konzendi.app` is provisional until something is distributed. See [Phase 0 decisions](phases/phase-0-repo-setup.md#decisions-and-evidence). | [Phase 0](phases/phase-0-repo-setup.md) |
| Q02 | Is the first deliverable a validation prototype or a usable product, and what is its smallest scope? | Before application scaffolding | Topics, switching, pauses, correction, and a timeline are proposed. | Resolved | A validation prototype testing whether switches get logged at all. Its feature scope is planned in a later phase; Phase 0 delivers only the foundation. See [Phase 0 decisions](phases/phase-0-repo-setup.md#decisions-and-evidence). | [Phase 0](phases/phase-0-repo-setup.md) |
| Q03 | Which delivery form and initial operating systems/browsers should be supported? | Before stack and tooling selection | Compare browser web app, PWA, Electron desktop, and Tauri desktop. | Resolved | Tauri v2 desktop application with a React + TypeScript + Vite frontend; Linux (X11) is the only initial target. See [Phase 0 decisions](phases/phase-0-repo-setup.md#decisions-and-evidence). | [Phase 0](phases/phase-0-repo-setup.md) |
| Q04 | Should the app be offline-only, local-first with online features, or online with accounts; what stack and tooling follow? | Before application scaffolding; after Q02 and Q03 | Decide accounts, data location, backup, sync, offline behavior, and any backend before choosing storage and development/check commands. | Resolved | Offline-only with no accounts and no backend. Data is a local append-only JSONL event log, one file per device, with event ids, device id, and recorded timestamps so synchronization stays additive. Rust owns durability; a pure TypeScript core owns domain logic and analytics. Checks: Biome, `tsc`, Vitest, `cargo fmt`, `cargo clippy`. See [Phase 0 decisions](phases/phase-0-repo-setup.md#decisions-and-evidence). | [Phase 0](phases/phase-0-repo-setup.md) |
| Q05 | What is the primary logging interaction, and how are pauses, accidental switches, missed entries, and completed work represented? | Before the tracking implementation plan is ready | Buttons, shortcut, tray, undo, and corrections are proposed. | Resolved | Selecting a topic is one action that ends the running interval and starts the next. Topics are entities with stable ids, created while logging, renameable and archivable. A pause is a distinct state that never ends automatically. Accidental switches and wrong times are corrected by `entry.revoked` and `entry.retimed` events referencing an earlier event id, folded deterministically. Missed entries are handled by a prominent elapsed time, back-dating, and inserting a past switch; nothing is automatic. Completed work is not distinguished from an interruption in this scope. The quick-access surface offers switching, pause, and undo of the last entry. See [Phase 1 decisions](phases/phase-1-tracking-design.md#decisions-and-evidence). | [Phase 1](phases/phase-1-tracking-design.md), [Phase 2](phases/phase-2-tracking-implementation.md) |
| Q06 | How will usefulness and logging friction be evaluated, and what result justifies further development? | Before planning a validation trial | A one-week trial is proposed; participants and success criteria are undecided. | Open | — | [Phase 5](phases/phase-5-validation-trial.md) |
| Q07 | Should the first scope include a score or effort levels? If so, what do they mean and how are they explained and tested? | Before score or effort implementation | No formula or validated interpretation exists. | Open | — | — |
| Q08 | Which analytics are useful enough to include initially? | Before the first analytics implementation plan | Timeline, uninterrupted intervals, switch rates, and trends are candidates. | Resolved | A day timeline of lanes — one per topic, pauses last, on a fixed 24-hour axis — plus three readings for that day: time recorded per topic, the number of changes, and the longest uninterrupted stretch. Lanes carry identity and colour stays state, so Phase 6's rule holds. An interval that nothing ended is drawn, marked, and counted, never trimmed. Median intervals and switch pairs are deferred until real logging exists to judge them. See [Phase 4 decisions](phases/phase-4-timeline-analytics.md#accepted-decisions). | [Phase 4](phases/phase-4-timeline-analytics.md) |
| Q09 | Should the product be commercial, and what are the free/paid boundary, price, and update entitlement? | Before commercial implementation | One-time Pro purchase is a proposal. | Open | — | — |
| Q10 | Which distribution and payment channels, device limits, and offline entitlement rules should apply? | Before release or licensing implementation | Direct licensing and an app store are alternatives, dependent on platform and Q09. | Open | — | — |
| Q11 | How should a hosted synchronization service carry tracking data between devices without being able to read it? | Before the synchronization phase becomes implementation-ready | The owner selected server-supported sync inspired by Obsidian's remote-vault model. The per-device append-only log is already shaped for additive merging. | Investigating | Hosted sync with password-based end-to-end encryption is the accepted direction. Encryption must happen on the client so the service never receives plaintext tracking data or a usable content key. Account, key-recovery, metadata, object-layout, and protocol decisions remain open; see [Phase 10](phases/phase-10-encrypted-sync.md). | [Phase 10](phases/phase-10-encrypted-sync.md) |
| Q12 | What visual direction should the application theme take, which modes must it support, and what accessibility floor applies? | Before the theme is applied | The current styling is an undocumented default palette with literal values, no tokens, and no light/dark handling. | Resolved | "Readout": the window is an instrument panel whose elapsed reading dominates the fold, colour carries tracking state rather than decoration, and switch targets show the number key that fires them. Chosen over two named alternatives, *Spine* and *Margin*. Thirteen colour roles are declared as custom properties; Fira Sans and Fira Mono are bundled locally under OFL-1.1. Light and dark both ship, following the desktop setting with an in-application override stored in `localStorage`, which appends no event. The floor is WCAG 2.2 AA, measured on rendered output. See [Phase 6 decisions](phases/phase-6-application-theme.md#decisions-and-evidence). | [Phase 6](phases/phase-6-application-theme.md) |
| Q13 | Which CI host, runners, required checks, and publication permissions should be used? | Before implementing CI workflows | GitHub API inspection confirms private `main`, Actions enabled, and no available merge protection in the current setup. | Resolved | GitHub Actions on Ubuntu 24.04, existing frontend/Rust checks plus package smoke; owner-operated preparation with a job-scoped token. No paid upgrade. Account budget and hosted execution remain rollout checks. See [Phase 7 decisions](phases/phase-7-releases-ci-cd.md#decisions-and-evidence). | [Phase 7](phases/phase-7-releases-ci-cd.md) |
| Q14 | What versioning, tag, changelog, release-channel, and promotion conventions should apply? | Before release automation | Owner superseded workflow-driven version updates with local preparation and a pushed tag trigger. | Resolved | Maintainer prepares versions and changelog, optionally using local prepare/check helpers, then commits and pushes `vVERSION`. CI checks the tagged commit, builds packages, and creates a retryable private draft; owner clicks Publish. No special commit format. See [Phase 7 decisions](phases/phase-7-releases-ci-cd.md#decisions-and-evidence). | [Phase 7](phases/phase-7-releases-ci-cd.md) |
| Q15 | How should prototype Linux packages be built, verified, and distributed, with what identity, licensing, and signing policy? | Before distributing artifacts | Owner selected private releases and accepted the initial package/signing scope. | Resolved | Private GitHub releases; unsigned x86_64 `.deb` for Ubuntu 24.04 / Mint 22 X11, SHA-256 checksums, existing data identity, third-party notices, and no new public licence. Commercial policy remains Q10. See [Phase 7 decisions](phases/phase-7-releases-ci-cd.md#decisions-and-evidence). | [Phase 7](phases/phase-7-releases-ci-cd.md) |
| Q16 | How are topic quick keys assigned, and how are topics without a key reached? | Before Phase 16 implementation | Creation-order numbering and pinning were discussed in Phase 3; the owner requested explicit stable assignments on 12 September 2026. | Resolved | Assign optional keys 1–9 in Topics, use the same keys in both views, allow gaps without empty layout slots, and expose unassigned topics through Other topics. No validation-trial dependency remains. See [Phase 16 decisions](phases/phase-16-topic-quick-keys.md#decisions-and-evidence). | [Phase 3](phases/phase-3-quick-access.md), [Phase 16](phases/phase-16-topic-quick-keys.md) |
| Q17 | Should the application draw its own window frame instead of the desktop's, what does that frame carry, and what happens where an undecorated window behaves badly? | Before the frame is implemented | The tracking window uses the desktop's title bar; the quick switcher is already undecorated. Phase 6 themed everything inside the frame but not the frame itself. | Resolved | Yes. The application's own bar absorbs today's header — name or back control, navigation, appearance override — and adds minimise, maximise/restore, and close, where close keeps hiding to the tray. A stored preference restores the desktop's decorations where the custom frame works badly. The quick switcher shares the border treatment and gains no controls. The frame carries no command beyond those: a right-click window menu and an always-on-top toggle were offered and declined. Platform behaviour on X11 remains to be measured; see [Phase 8 decisions](phases/phase-8-window-frame.md#decisions-and-evidence). | [Phase 8](phases/phase-8-window-frame.md) |
| Q19 | Which Windows and macOS versions, architectures, packages, signing policy, and desktop-integration behavior should Konzendi support for private trials? | After the Phase 7 release pipeline is implemented; before producing non-Linux trial builds | Tauri supplies a cross-platform base, but the quick switcher, focus handoff, tray behavior, packaging, and verification are currently Linux/X11-specific or untested elsewhere. | Deferred | Windows and macOS support is worth investigating so other people can try Konzendi, but it follows the Linux release pipeline. Phase 11 must gather platform evidence and record the exact support and distribution contract before implementation. | [Phase 11](phases/phase-11-windows-macos.md) |
| Q20 | What logo system should represent Konzendi in a desktop tray and in repository documentation? | Before adopting a logo | The running application uses generated Tauri icons as both its window and tray icon; README has no brand asset. Q01 records `Konzendi` as the public product name. | Open | — | [Phase 12](phases/phase-12-logo-design.md) |
| Q21 | Which further statistics earn a place once real logging exists, and over what span? | After [Phase 5](phases/phase-5-validation-trial.md) produces logged data; before any second set of readings is implemented | Phase 4 shipped the smallest set and deferred the median uninterrupted stretch and the most-switched topic pairs by name. Switch rates, planned transitions, and trends remain concept proposals. | Open | — | [Phase 14](phases/phase-14-further-statistics.md) |
| Q22 | Is a pause a state worth recording as data, given that it is also the only way to stop tracking? | Raised by the first analytics; before the analytics are used in a trial | The owner's log held a stop of 42 hours, and the analytics reported it as 15:55 recorded as paused and marked it as possibly forgotten. Phase 1 had already decided there is no separate stop action. | Resolved | No. There is one way to stop and it is called Stop: the word, the `■` mark, and the `s` keystroke replace pause on every surface, while the stored kind stays `focus.paused` because its meaning is unchanged. Stopped time is absence in the analytics — no lane, no sum, no mark — and `Switches` counts only the entries that started work on a topic. No new event kind. See [Phase 13 decisions](phases/phase-13-stop-replaces-pause.md#accepted-decisions). | [Phase 13](phases/phase-13-stop-replaces-pause.md) |
| Q23 | Which topics does the window's switch list contain, and what happens to a number when a topic runs or tracking stops? | Raised from the running window; before the numbers are relied on in a trial | The quick switcher lists every topic that is not archived and keeps each number fixed; the window left out the running topic and the one offered for resume, so the same topic had different numbers on the two surfaces. Phase 2 recorded this as an open wart. | Resolved | Both surfaces list every topic that is not archived, in creation order, and a number never moves. The running row stays in the list and is marked with `▶` and the word "running"; selecting it is coalesced by the fold. The Resume control is unchanged. Behaviour beyond nine topics stays Q16. See [Phase 15 decisions](phases/phase-15-stable-topic-numbering.md#accepted-decisions). | [Phase 15](phases/phase-15-stable-topic-numbering.md) |

Only decisions needed by the next work should block it. Commercial decisions need not delay
an initial tracking experiment unless the chosen scope depends on them.
